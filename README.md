# hue-presence
Update your Phillips Hue lights based on your Presence API color

## Note
This depends on the [PresenceBot API](https://github.com/EricRabil/PresenceBot) being deployed and available.


## Running
1. Clone the repository
2. Run `npm i` or `yarn`
3. Run `node js/run.js`